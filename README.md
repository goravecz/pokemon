## Requirements

- Java 21 or higher
- Maven 3.6+

## Build & Run

### Compile the project
```bash
mvn clean compile
```

### Run the demo application
```bash
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```